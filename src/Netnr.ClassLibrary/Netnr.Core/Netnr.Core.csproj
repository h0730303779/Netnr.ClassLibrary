<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFrameworks>net40;netstandard2.1</TargetFrameworks>
    <Description>开发常用的核心类库；
使用文档看GitHub（Gitee）项目说明</Description>
    <Copyright>netnr</Copyright>
    <PackageIconUrl>https://raw.githubusercontent.com/netnr/static/master/favicon.ico</PackageIconUrl>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <PackageProjectUrl>https://github.com/netnr/Netnr.ClassLibrary</PackageProjectUrl>
    <RepositoryUrl>https://github.com/netnr/Netnr.ClassLibrary</RepositoryUrl>
    <PackageLicenseUrl></PackageLicenseUrl>
    <PackageTags>netnr</PackageTags>
    <PackageReleaseNotes>- 调整 项目框架为 `.NET Standard 2.1`
- 调整 `CmdTo.Shell` 方法，输出`BashResult`对象，直接集成`Shell.NET`源码去除引用
- 调整 `ClientTo` 类转移到 `Netnr.Fast` , 即删除 `Core.ClientTo`
- 调整 `DownTo` 类转移到 `Netnr.Fast` , 即删除 `Core.DownTo`
- 调整 `ConsoleTo.Log` 方法，支持`object`输出，内部自动转换
- 删除 `MapPathTo` 类</PackageReleaseNotes>
    <Authors>netnr</Authors>
    <Version>1.2.0</Version>
    <DocumentationFile>D:\ROOM\Netnr.ClassLibrary\src\Netnr.ClassLibrary\Netnr.Core\Netnr.Core.xml</DocumentationFile>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)|$(TargetFramework)|$(Platform)'=='Debug|net40|AnyCPU'">
    <DefineConstants>TRACE;DEBUG;NET40</DefineConstants>
    <PlatformTarget>AnyCPU</PlatformTarget>
  </PropertyGroup>
  
  <ItemGroup>
    <PackageReference Include="Newtonsoft.Json" Version="12.0.2" />
  </ItemGroup>

  <ItemGroup Condition="'$(TargetFramework)' == 'netstandard2.1'">
    <PackageReference Include="Microsoft.Extensions.Caching.Memory">
      <Version>3.0.0</Version>
    </PackageReference>
  </ItemGroup>

  <ItemGroup Condition="'$(TargetFramework)' == 'net40'">
    <Reference Include="System.Web" />
  </ItemGroup>

</Project>
